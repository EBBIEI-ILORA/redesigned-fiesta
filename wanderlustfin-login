<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login — WanderlustFin</title>
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=Jost:wght@300;400;500&display=swap" rel="stylesheet">
  
  <!-- Styles -->
  <link rel="stylesheet" href="css/styles.css">
  
  <style>
    .login-page {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-xl);
      background: linear-gradient(180deg, var(--cream-warm) 0%, var(--cream) 100%);
    }

    .login-container {
      width: 100%;
      max-width: 420px;
    }

    .login-header {
      text-align: center;
      margin-bottom: var(--space-xl);
    }

    .login-header .logo {
      font-size: 2rem;
      margin-bottom: var(--space-md);
      display: block;
    }

    .login-header p {
      color: var(--text-muted);
    }

    .login-box {
      background: var(--cream-warm);
      padding: var(--space-xl);
      box-shadow: 0 10px 40px rgba(61, 66, 72, 0.08);
    }

    .login-box h2 {
      font-size: 1.5rem;
      margin-bottom: var(--space-lg);
      text-align: center;
    }

    .login-form .form-input {
      background: white;
    }

    .login-form .btn {
      width: 100%;
      margin-top: var(--space-sm);
    }

    .login-footer {
      text-align: center;
      margin-top: var(--space-lg);
      padding-top: var(--space-lg);
      border-top: 1px solid var(--stone-light);
    }

    .login-footer p {
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    .login-footer a {
      color: var(--hydrangea-deep);
    }

    .forgot-password {
      text-align: right;
      margin-top: var(--space-xs);
    }

    .forgot-password a {
      font-size: 0.85rem;
      color: var(--taupe);
    }

    .divider {
      display: flex;
      align-items: center;
      margin: var(--space-lg) 0;
    }

    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--stone-light);
    }

    .divider span {
      padding: 0 var(--space-md);
      font-size: 0.8rem;
      color: var(--taupe);
    }

    .coming-soon-notice {
      background: var(--sage);
      color: var(--cream-warm);
      padding: var(--space-md);
      text-align: center;
      margin-bottom: var(--space-lg);
    }

    .coming-soon-notice p {
      color: var(--cream-warm);
      font-size: 0.9rem;
      margin: 0;
    }

    .back-home {
      text-align: center;
      margin-top: var(--space-lg);
    }

    .back-home a {
      font-size: 0.85rem;
      color: var(--taupe);
    }
  </style>
</head>
<body>

  <div class="login-page">
    <div class="login-container">
      <div class="login-header">
        <a href="index.html" class="logo">Wanderlust<span>Fin</span></a>
        <p>Your Wealth Identity Operating System</p>
      </div>

      <div class="login-box">
        <div class="coming-soon-notice">
          <p>✨ We're launching soon! Join the waitlist for early access.</p>
        </div>

        <h2>Welcome back</h2>

        <form class="login-form" action="#" method="POST">
          <div class="form-group">
            <label class="form-label" for="email">Email Address</label>
            <input type="email" id="email" name="email" class="form-input" placeholder="you@example.com" required>
          </div>
          
          <div class="form-group">
            <label class="form-label" for="password">Password</label>
            <input type="password" id="password" name="password" class="form-input" placeholder="••••••••" required>
            <div class="forgot-password">
              <a href="#">Forgot password?</a>
            </div>
          </div>

          <button type="submit" class="btn btn-primary" disabled>Login</button>
        </form>

        <div class="divider">
          <span>or</span>
        </div>

        <a href="waitlist.html" class="btn btn-secondary" style="width: 100%; text-align: center;">Join the Waitlist</a>

        <div class="login-footer">
          <p>Don't have an account? <a href="waitlist.html">Get early access</a></p>
        </div>
      </div>

      <div class="back-home">
        <a href="index.html">← Back to home</a>
      </div>
    </div>
  </div>

</body>
</html>
